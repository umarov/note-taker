version: "3"
services:
  app:
    build: .
    depends_on:
      - postgres
    ports:
      - "3000:3000"
  postgres:
    image: postgres:11-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: note-taker
      POSTGRES_USERNAME: postgres
      POSTGRES_PASSWORD: postgres
